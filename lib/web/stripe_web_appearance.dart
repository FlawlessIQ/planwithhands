import 'package:flutter/material.dart';

/// Central place for any web-specific appearance logic for embedded iframes.
class StripeWebAppearance {
  /// Returns a background color for CardField container on web to ensure text contrast.
  static const Color cardContainer = Colors.white;
  static const Color cardBorder = Colors.black12;
  static const Color textColor = Colors.black87;
  static const Color hintColor = Colors.black54;
}
